# Weekly log review — 2026-08-14 (covering Aug 9–14)

Logs pulled: `debugging/daily_log_0814.txt` (3.4 MB, rotated — contains Aug 10–14 only), `debugging/weekly_log_0814.txt` (Sunday Aug 9 run), `debugging/server_log_0814.txt` (**0 bytes — empty; check whether server.log rotated or Gunicorn logs elsewhere**).

## Overall

All 54 users got their daily digests every day — every daily failure was recovered by the automatic post-run retry pass within ~5 minutes. Two weekly digests on Friday Aug 14 were **not** recovered (known bug: weekly-send failures invisible to retry), and one Sunday weekly-only user has a broken `.env`.

## Failure breakdown

### 1. Gmail SMTP 421 "Temporary System Problem" — dominant failure mode (~17 send failures over 5 days)
Transient Gmail-side throttling at `send_email` time, hitting bursts of users sending in parallel:
- Aug 11: 5 daily sends (berkandorukeke, yuval, seileran, photocurrent, Taka)
- Aug 12: 2 daily (eudomar-henriquez, joepheerenveen)
- Aug 13: 1 daily (alexander-rothstein)
- Aug 14: 6 daily (nadav-shemesh5, yuval, shahal-ilani, tamar-levin, noam-ophir, zerui-qian) + 3 weekly (noam-ophir, **amitinbar-mof-gov-il, yanirmr-gmail-com**)

All daily 421s auto-recovered. Frequency is increasing (54 users in parallel via ThreadPoolExecutor all hammering smtp.gmail.com at once) → consider retry-with-backoff on SMTP 421 inside `send_email()`, or serializing/staggering sends. Added to TODO.

### 2. Missed weekly digests Aug 14 — NOT recovered ⚠️
`amitinbar-mof-gov-il` (weekly-only user — got nothing this week) and `yanirmr-gmail-com` (daily was fine) failed only their weekly send, so the retry pass (which keys off daily failures) never picked them up. This is the known TODO bug "weekly-send failures invisible to run_failed_users."

**Recovery (run on server):**
```bash
python run_weekly_digest.py --user-dir users/amitinbar-mof-gov-il --date 2026-08-14
python run_weekly_digest.py --user-dir users/yanirmr-gmail-com --date 2026-08-14
```
Note: the 4 weekly users who ALSO failed daily (noam-ophir, nadav-shemesh5, zerui-qian, shahal-ilani) DID get their weekly resent by the retry pass — weekly summary shows OK.

### 3. `anton-sv-campus-technion-ac-il` weekly failed Sun Aug 9 — config error, permanent ⚠️
"Neither EMAIL_TO_WEEKLY nor EMAIL_TO is set in the user's .env". PDF was built, email never sent; fails every Sunday.

**Root cause (systemic):** his `.env` has only `EMAIL_TO_DAILY` — signup writes email vars only for the frequencies chosen at signup (`process_pending.py`), and the `/manage` frequency toggle flips profile flags without syncing `.env`. Any user enabling a frequency they didn't sign up with breaks the same way. **Fixed 2026-08-14** in `server.py`: `/manage/update-frequency` now ensures the env var for each enabled frequency via `_ensure_email_env_var()` (see [[Manage Profile]]). Anton himself needs the one-time manual line: `EMAIL_TO_WEEKLY=anton.sv@campus.technion.ac.il` appended to his `.env` (the code fix only heals future toggles). Also worth running the fleet audit for other daily↔weekly switchers.

### 4. Truncated-JSON scoring failures × 3 — known backlog item
- Aug 10: Yael (insights user; response truncated mid-`arxiv_id`)
- Aug 11: sudha-godhani1 and nadav-shemesh5 (both insights-style output, truncated mid-string)

All three auto-recovered on retry (at 2× scoring cost for those users). Three hits in one week strengthens the backlog item "Retry on truncated JSON in scoring" (`TODO.md`). All match the 2026-06-04 signature: output well under the 16k token cap, cut mid-string.

### 5. Semantic Scholar batch 429 on 4 of 5 days
`S2 batch returned 429` (chunks of 26–53 IDs) on Aug 10, 12, 13, 14 → "filled 0/N missing abstracts". S2 enrichment is effectively dead this week; abstract coverage rests on OpenAlex alone. Strengthens the case for the CORE API fallback (planned in TODO backlog).

## Journal/publisher systematic failures (checked Aug 15)

**ACS — all 7 journals dead since ~Jul 23** ⚠️ (NanoLett, ACSNano, ACSPhotonics, ACSSensors, Langmuir, Macromolecules, Biomacromolecules). Daily `feed parse error — <unknown>:4:75 not well-formed`, watermarks stuck at 2026-07-22/23.

**ROOT CAUSE (diagnosed same day):** not Cloudflare — server-side FlareSolverr probe showed the feed URL returns a plain **404**. ACS migrated its platform from Atypon to **Silverchair on 2026-07-24**, killing all `action/showFeed` RSS endpoints; their migration page lists RSS re-enablement as still forthcoming. **FIX (implemented 2026-08-14):** switched all 7 ACS journals to CrossRef enumeration — see [[Journal Scrapers]] § "ACS RSS feeds killed by Silverchair migration" for full details. Local test passed; server deploy + verify pending.

**ACM TOSEM — dead all week** (`2:1326 not well-formed` daily, 0 papers). `dl.acm.org` is NOT in `_CLOUDFLARE_HOSTS` (`scrapers/sources.py:41`) — likely Cloudflare-challenged on direct fetch. Candidate fix: add `dl.acm.org` to the FlareSolverr host set.

**IEEE SPL + TSE — 0 papers all week, suspicious watermark behavior** ⚠️. SPL: "0 articles scraped (skipped 50 at or before watermark)" every day — the entire 50-entry feed skipped — yet the watermark *advances daily* (08-08→08-09→08-10→08-11). If all entries were truly ≤ watermark, it shouldn't advance. Looks like feed entries carry a rolling/feed-build date, so everything is forever "at or before watermark" → permanent zero. TSE: watermark stuck 2026-08-05, all 100 entries skipped daily. Needs code investigation (`fetch_from_rss` date handling; related to audit finding "undated feed entries bypass watermark").

**Zeros that look like normal cadence (not failures):** MUSE (quarterly issues, watermarks advancing), AEA via CrossRef (publisher tag 'unknown' — AER/AEJ:Policy are monthly), Royal Society (0 on 4/5 days but 17 papers once — weekly issue drop), science/pnas/cambridge (episodic, papers seen this week).

## Non-issues checked
- No batch fallbacks, no Anthropic rate-limit hits, no triage/scoring API errors besides the JSON parses.
- Banked-paper retry (TTL 21d) running normally, ~100–125 papers/day.
- Sunday weekly-only run otherwise clean (6/7 OK).

## Follow-ups
- [ ] Resend 2 missed Aug-14 weekly digests (commands above)
- [ ] Fix anton-sv `.env` (missing EMAIL_TO)
- [ ] SMTP 421 retry/backoff in send_email (new TODO entry)
- [ ] server.log empty — verify log path/rotation next pull
