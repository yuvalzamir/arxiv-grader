# Run notes — 2026-08-25 (Tue), incl. 2026-08-24 (Mon)

Log pulled: `debugging/daily_log_0825.txt` (9,888 lines; Mon run = lines 1–5054, Tue run = 5055–end). Both runs post-date the 2026-08-21 deploy of the SMTP-421-retry and TOSEM-FlareSolverr fixes. 55 users (library-science user marta-serrat-upc-edu now live — her first arXiv triage ran on 2 cs.DL papers).

## 1. Fix verification — both fixes CONFIRMED WORKING

**ACM TOSEM via FlareSolverr:** Mon 08-24 — feed fetched and parsed for the first time since the item was filed: `22 articles scraped (skipped 22 at or before watermark)`, ID watermark advanced 3789210 → 3832776. Tue 08-25 — 0 new (all 44 entries at/before watermark), normal.

**SMTP 421 retry:** Tue 08-25 00:55:10 — five simultaneous Gmail `421 Temporary System Problem` rejections, each logged as `SMTP send failed (attempt 1/3) … retrying in 30s`. Zero `attempt 2/3` lines → every retry succeeded on the second attempt. No user failed on email delivery. Pre-fix this would have been ~5 FAILED users.

## 2. User report: no arXiv papers in yuval's Tue digest — NOT A BUG

Mechanics (users attributed to untagged triage lines via their per-user profile input-token counts, e.g. yuval = 1188 tokens in both triage stages):
- Tue arXiv fetch was healthy: cond-mat 122 + physics.optics 23 = 145 papers into the cond-mat-optics shared cache.
- yuval's Triage-arXiv returned a complete, well-formed response: `high: 0, medium: 0, low: 145`. Journals triage filled 10/10 → digest was journals-only.
- Not a cache or pipeline fault: same cached call seconds earlier gave photocurrent `high: 2, medium: 10` (10/10 arXiv forwarded); shahal-ilani got 2/10.
- Context: Mon yuval got 10/10 arXiv (11 qualifying of ~100 papers); Tue 0 of 145. 17 of 55 users had 0 arXiv Tue vs 11 on Mon — within normal variation under the strict concrete-anchor triage rule.

Verdict: legitimate all-low day for this profile. Optional follow-up if it recurs: pull `users/yuval/data/2026-08-25/triage_arxiv_input.txt` and spot-check whether any paper plausibly matched profile anchors (triage-quality question, not a crash).

## 3. New findings

**FlareSolverr intermittent failures (Tue only):** 10 × `FlareSolverr status=error` (SAGE ×4, T&F ×6) between 00:32–00:44; Mon had 0. TOSEM (dl.acm.org) went through fine at 00:30:50, so the container was up — Cloudflare challenges failed intermittently. Consequences Tue: gender-studies got **no papers from any source** (2 users skipped); several edu-policy/econ-education T&F feeds missed. Date-watermarked feeds → papers recovered next run, not lost. Action: monitor; if errors persist, restart the container (`docker restart flaresolverr`).

**ruihaoliu2001 FAILED → auto-recovered (5th truncated-JSON occurrence, NEW root cause):** scoring output was **15,671 tokens ≈ the 16,000 `max_tokens` cap** with 20 insights papers → genuine cap truncation, unlike the earlier occurrences (e.g. 2026-06-04: 6,153 tokens, transient API fault). The TODO rough-edge note "16000 sufficient for ~30 papers" predates the insights rollout and is wrong: 20 insights papers can exceed it. `run_failed_users.py` auto-recovered (email sent 01:02:42) at 2× cost. Strengthens the retry-on-truncation TODO item; also consider raising `max_tokens` for scoring (insights ≈ 500–800 tokens/paper × 20 papers ≥ 16k).

**Minor:** `econ.LM` arXiv feed empty (tiny category, normal); one `S2 batch returned 400 for chunk of 8 IDs`; quiet journals-only fields (gender-studies, literature-and-culture, music-theory) skipped cleanly.

## Status summary

STATUS: Complete (55/55 users OK after auto-retry; Tue had 1 transient failure, auto-recovered).
