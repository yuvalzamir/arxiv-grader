# Daily log review — 2026-08-17 (Monday)

Log pulled: `debugging/daily_log_0817.txt` (~5.4k lines, this run only). Purpose: verify the ACS → CrossRef switch on its first cron run, plus routine once-over.

## Headline

**ACS → CrossRef switch confirmed working in production**: `Publisher 'acs': 2 paper(s) collected` at 00:30:25. Low count is expected — the 83-paper backlog was consumed by the 08-14 server test, so this is one weekend's worth of new CrossRef registrations. TODO entry deleted.

All 55 users fine: 48 OK first pass, 6 FAILED → all 6 recovered by the automatic retry pass by 00:49, 1 NO-RUN (cedric, music-theory quiet day — no papers from any source, 1 user skipped). 12 weekly digests (Monday cohort) all OK.

## Failures (all auto-recovered)

- **Gmail SMTP 421 × 5** (QED, kt51-illinois-edu, shahal-ilani-weizmann-ac-il, noam-ophir-campus-technion-ac-il, mcagetti-icfo-net) — same parallel-burst throttling as last week (~17 hits Aug 10–14). Retry pass resent all 5. Backlog item "Retry SMTP 421 in send_email" keeps gaining evidence.
- **Truncated-JSON scoring × 1** (Ediz, insights user) — response cut mid-string in a `justification` field at 4,856 output tokens (way under the 16k cap); identical signature to the 2026-06-04 / Aug 10–11 incidents. Auto-recovered at 2× scoring cost. That's 5 occurrences total, all insights users. Backlog item "Retry on truncated JSON in scoring".

## Journal/publisher notes

- **IEEE TSE recovered on its own**: 17 articles scraped, watermark advanced 2026-08-05 → 2026-08-14. Last week's "stuck watermark" for TSE was apparently just no new content in the feed. **IEEE SPL still permanently zero**: watermark 2026-08-15, all 50 entries "skipped at or before watermark" while the watermark keeps advancing — the contradictory pattern remains, now SPL-specific. TODO entry narrowed to SPL.
- **ACM TOSEM still dead** (`2:1326 not well-formed` parse error) — known, FlareSolverr candidate fix in TODO.
- **S2 enrichment recovered** after last week's 429 streak: `S2 batch: filled 8/17 missing abstracts`.
- Bank healthy: 104 banked papers retried, 9 abstracts recovered, 7 injected across 6 fields; 8 new papers banked.
- Quiet-but-normal: cambridge/royalsociety/acm 0 (cadence), music-theory field empty today.

## Follow-ups carried from [[2026-08-14]]

- [x] **anton-sv's first weekly digest confirmed** (`weekly_log_0817.txt`): Sunday 08-16 run, 3 papers ≥8, PDF built, email sent, summary OK — the `/manage` `.env` sync fix works end-to-end. All 7 Sunday weekly-only users OK (4 digests, 3 no-papers notifications).
- [ ] server.log was empty on last pull — verify log path/rotation on next `server.log` pull
